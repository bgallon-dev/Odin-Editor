[pytest]
testpaths = tests
markers =
    lm: requires LM Studio with models loaded
    slow: takes more than 10 seconds
    eval: evaluation test — records scores, soft failure
addopts = -v --tb=short
