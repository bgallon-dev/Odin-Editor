[pytest]
testpaths = tests
markers =
    lm: requires LM Studio running with both models loaded
    slow: takes more than 10 seconds
    eval: evaluation test — records scores, soft failure threshold
addopts = -v --tb=short
